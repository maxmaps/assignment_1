// create an array or object with a list of your favorite things to do in your free time
// then, create a loop that prints each item in the console

var favoritethings = ["guitar", "movies", "eating"];

for (var i = 0; i < favoritethings.length; i++) {
    console.log(favoritethings[i]);
}